[user]
    name = saqula
    email = ping@saqu.la
    signingkey = 01F88430C644881C

[color]
    diff = auto        # 差分の表示を自動で色付け
    status = auto      # ステータスの表示を自動で色付け
    branch = auto      # ブランチ名の表示を自動で色付け
    interactive = auto # インタラクティブな操作の表示を自動で色付け
    grep = auto        # grepコマンドの出力を自動で色付け
    ui = auto          # Git UIの表示を自動で色付け

# GPG署名の設定
[commit]
    gpgSign = false
    template = ~/.config/git/message

[tag]
    gpgSign = false
[gpg]
    format = openpgp
    program = gpg

[url "git@github.com:"]
    insteadOf = https://github.com/

[ghq]
    root = ~/Documents/02_codes
